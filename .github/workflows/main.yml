name: First Workflow

on: 
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a Welcome message
        run: echo "Welcome sir, what action should i run"
        
      - name: checkout repository
        uses: actions/checkout@v6.0.2
        
      - name: list repository files
        run: ls -al
