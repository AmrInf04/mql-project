name: First Workflow

on: 
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a Welcome message
        run: echo "Welcome sir, what action should i run"
